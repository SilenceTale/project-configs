server:
  port: 3337

spring:
  # 데이터 베이스 설정
  datasource:
    driverClassName: oracle.jdbc.driver.OracleDriver
    url: jdbc:oracle:thin:@${db.host}:XE
    username: ${db.username}
    password: ${db.password}
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true

cors:
  allowed-origins:
    - https://ksg-eureka-service.silvertale.xyz
    - localhost:3002

swagger:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html
